# =============================================================================
# Development Environment Overrides
# =============================================================================

app:
  debug: true
  cors_origins:
    - "http://localhost:3000"
    - "http://localhost:5173"  # Vite default
    - "http://127.0.0.1:3000"

database:
  echo: true  # Show SQL queries in dev

logging:
  level: "DEBUG"
